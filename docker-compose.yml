version: '3'
services:
  worker:
    build: .
    restart: always
    volumes:
    - /var/run/docker.sock:/var/run/docker.sock
    - ~/.gcpcreds/:/root/.gcpcreds
    environment:
      GOOGLE_APPLICATION_CREDENTIALS: /root/.gcpcreds/silken-impulse-217423-8fbe5bbb2a10.json
